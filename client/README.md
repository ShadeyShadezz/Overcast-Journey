OvercastJourney — client

Local dev:

1. Install deps: `npm install`
2. Start dev server: `npm run dev`

This is a Vite + React app with a minimal UI and WebRTC logic focused on connectivity.
Set `VITE_SIGNALING_URL` to point to the signaling server if needed.
